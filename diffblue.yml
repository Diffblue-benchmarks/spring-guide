buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
#Target TGF: 28.6666666667
# - methods: 7
# - complexity: 70
# - construction: 3.66666666667
# - dependency: 6.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/model/Address$AddressBuilder.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/model/Address.java
#Target TGF: 29.5
# - methods: 8
# - complexity: 68
# - construction: 3.33333333333
# - dependency: 2.33333333333
  - target:
    - dir: core/target/classes
      file: com/spring/guide/model/Name.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/model/Name.java
#Target TGF: 26.5333333333
# - methods: 7
# - complexity: 70
# - construction: 3.4
# - dependency: 6.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/domain/coupon/Coupon$CouponBuilder.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/domain/coupon/Coupon.java
#Target TGF: 58.4523809524
# - methods: 14
# - complexity: 133
# - construction: 3.47619047619
# - dependency: 3.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/domain/member/Member.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/domain/member/Member.java
#Target TGF: 22.8666666667
# - methods: 15
# - complexity: 48
# - construction: 4.53333333333
# - dependency: 5.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/domain/coupon/Coupon.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/domain/coupon/Coupon.java
#Target TGF: 29.0
# - methods: 7
# - complexity: 60
# - construction: 3.33333333333
# - dependency: 2.5
  - target:
    - dir: core/target/classes
      file: com/spring/guide/model/Email.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/model/Email.java
#Target TGF: 20.8
# - methods: 6
# - complexity: 58
# - construction: 2.93333333333
# - dependency: 5.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/domain/member/Member$MemberBuilder.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/domain/member/Member.java
#Target TGF: 18.0666666667
# - methods: 3
# - complexity: 40
# - construction: 1.53333333333
# - dependency: 2.5
  - target:
    - dir: api-service/target/classes
      file: com/spring/guide/coupon/CouponApi.class
      sourceDir: api-service/src/main/java
      sourceFile: com/spring/guide/coupon/CouponApi.java
#Target TGF: 24.0
# - methods: 6
# - complexity: 58
# - construction: 3.33333333333
# - dependency: 5.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/model/Name$NameBuilder.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/model/Name.java
#Target TGF: 14.6666666667
# - methods: 8
# - complexity: 30
# - construction: 2.33333333333
# - dependency: 2.0
  - target:
    - dir: core/target/classes
      file: com/spring/guide/model/Address.class
      sourceDir: core/src/main/java
      sourceFile: com/spring/guide/model/Address.java
dependenciesOnClasspath:
-
  source: classes
  classFile: com/spring/guide/model/Address$AddressBuilder.class
-
  source: classes
  classFile: com/spring/guide/coupon/CouponResponse.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/exception/CouponAlreadyUseException.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/CouponHelperService.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/Coupon$CouponBuilder.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/CouponCode.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/Coupon.class
-
  source: classes
  classFile: com/spring/guide/model/Name$NameBuilder.class
-
  source: classes
  classFile: com/spring/guide/domain/member/Member.class
-
  source: classes
  classFile: com/spring/guide/domain/member/Member$MemberBuilder.class
-
  source: classes
  classFile: com/spring/guide/model/Address.class
-
  source: classes
  classFile: com/spring/guide/coupon/CouponUseService.class
-
  source: classes
  classFile: com/spring/guide/domain/coupon/exception/CouponExpireException.class
-
  source: classes
  classFile: com/spring/guide/model/Name.class
-
  source: classes
  classFile: com/spring/guide/model/Email.class